type Maybe a = Just a | Nothing

fn is_just m =
  match m
    | Just _ -> true
    | Nothing -> false

fn from_int n =
  if n > 0 then Just n else Nothing

fn main =
  let x = Just 42
  let y = from_int 0
  println (is_just x)
  println (is_just y)
  println "Generic types work!"
