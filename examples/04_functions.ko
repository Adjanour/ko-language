# Functions — First-class functions, recursion.

fn apply f x = f x
fn double x = x * 2
fn add a b = a + b
fn factorial n =
  if n == 0 then 1
  else n * factorial (n - 1)

fn main =
  let a = double 5
  let b = add 1 2
  let c = factorial 5
  a + b + c
