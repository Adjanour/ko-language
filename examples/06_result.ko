// 06_result.ko — Result type for error handling

type Result = Ok * | Err *

fn divide a b =
  if b == 0 then Err "division by zero"
  else Ok (a / b)

fn show-result r =
  match r
    Ok v -> println v
    Err msg -> println msg

fn main =
  show-result (divide 10 2)
  show-result (divide 10 0)
