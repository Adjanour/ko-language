# Algebraic Data Types — Model your domain with types.

type Maybe a = Just a | Nothing
type Result a b = Ok a | Err b
type Bool = True | False

fn main =
  let x = Just 42
  let y = Ok "hello"
  let z = True
  1
