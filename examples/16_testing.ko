// Testing — Built-in test primitives for verified code.

fn add a b = a + b
fn multiply a b = a * b

fn main =
  test "addition" (add 2 3 == 5)
  test "multiplication" (multiply 4 5 == 20)
  test "identity" (add 0 5 == 5)

  assert (1 + 1 == 2)
  assert_eq (multiply 3 4) 12

  run_tests
