# Pipe Operator — Chain functions left-to-right.

fn double x = x * 2
fn add10 x = x + 10
fn negate x = 0 - x

fn main =
  let a = 5 |> add10 |> double
  let b = 3 |> double |> add10 |> negate
  a + b
